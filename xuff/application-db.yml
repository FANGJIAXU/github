spring:     
    datasource:
      druid:
        url: jdbc:mysql://10.202.229.122:3306/19test0321?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
        username: root
        password: sa123
        initial-size: 10
        max-active: 100
        min-idle: 10
        max-wait: 60000
        pool-prepared-statements: true
        max-pool-prepared-statement-per-connection-size: 20
        time-between-eviction-runs-millis: 60000
        min-evictable-idle-time-millis: 300000
        validation-query: SELECT 1 FROM DUAL
        test-while-idle: true
        test-on-borrow: false
        test-on-return: false
        stat-view-servlet:
          enabled: true
          url-pattern: /druid/*
          login-username: admin
          login-password: admin
        filter:
          stat:
            log-slow-sql: true
            slow-sql-millis: 1000
            merge-sql: true
          wall:
            config:
            multi-statement-allow: true
    redis:
      host: 10.202.229.122
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          min-idle: 0
          max-idle: 8
    data:
      mongodb:
        host: 10.202.229.122
    rabbitmq:
      host: 10.202.229.122
      username: guest
      password: guest
feign:
    hystrix:
      enabled: true
    connectTimeOutMillis: 5000
    readTimeOutMillis: 5000
hystrix:
    command:
      default:
        execution:
          isolation:
            strategy: SEMAPHORE
            thread:
              timeoutInMilliseconds: 5000
mybatis-plus:
    global-config:
        metaObjectHandler: com.maxnerva.cloud.datasource.handler.MaxMetaObjectHandler
    mapper-locations: mapper/*Mapper.xml
    configuration:
        call-setters-on-nulls: true

thread:
    pool:
      corePoolSize: 10
      maxiPoolSize: 20
      keepAliveTime: 0






